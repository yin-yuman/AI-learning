# AlexNet论文学习笔记

## 1. 论文基本信息

标题：ImageNet Classification with Deep Convolutional Neural Networks

作者：Alex Krizhevsky \ Ilya Sutskever \ Geoffrey E. Hinton

## 2. 研究背景

随着计算机视觉技术的发展，物体识别任务逐渐从依赖人工设计特征的方向转向基于机器学习的方向，早期机器学习方法收到标注数据规模的限制，难以充分学习复杂的知识，要从更大规模的数据集中进行学习就需要一个具有更强大学习能力的模型。

## 3. 研究问题

如何设计一个能够在更大规模的数据集中进行学习的模型。

## 4. 核心思想

AlexNet通过构建更深的CNN网络，结合ReLU激活函数、跨GPU的并行化方案、重叠池化、Dropout等技术，得到了更低错误率的大规模图像分类。

## 5. 方法详解

### 5.1 CNN结构

该网络共有八个学习层——其中五个为卷积层，三个为全连接层。

### 5.2 ReLU激活函数

研究显示，采用饱和非线性函数的训练速度远慢于非饱和非线性函数，因此作者使用ReLU作为神经元激活函数

### 5.3 跨GPU的并行化方案

因为单个GPU的现存有限，限制了训练网络的最大规模，单个GPU难以完成大规模CNN训练，因此作者采用两个GPU进行并行训练

### 5.4 重叠池化

传统上由相邻池化单元所汇总的领域之间不会存在重叠，如果设定单元之间的间距s<尺寸z即可得到重叠池化

### 5.5 Dropout

如果不使用dropout，网络会出现显著的过拟合现象，dropout会使得收敛所需的迭代次数翻倍

## 6. 模型结构

![AlexNet](./基于深度卷积神经网络的ImageNet分类/images/struct.png)

如上图所示，该CNN网络包含8个带权重的层，其中前5层为卷积层，后3层为全连接层

## 7. 实验分析

### 7.1 数据集

实验使用ImageNet数据集，包括120万个训练样本和1000个类别

### 7.2 训练方法

使用随机梯度下降训练模型（SGD）

### 7.3 实验过程

为所有层采用相同的学习率0.01，当验证误差率在当前的学习率下不再改善时，将学习率除以10

### 7.4 实验结果

分辨的错误率（37.5%、17.0%）远低于其他模型（47.1%、28.2%）

## 8. 创新点

### 8.1 激活函数创新

使用ReLU代替 tanh/sigmoid 单元，训练速度变快了6倍，让深层CNN训练成为可能

### 8.2 多GPU并行处理

给每个GPU都分配半数的卷积核并让各GPU在特定层之间进行通信该方案可以将Top-1和Top-5的错误率降低1.7%和1.2%，其训练实践也略短于单GPU网络

### 8.3 重叠池化

设定s=2，z=3与s=z=2相比可以将Top-1和Top-5的错误率降低0.4%和0.3%

### 8.4 Dropout

以0.5的概率将每个隐藏神经元的输出置零，这样能够减少神经元之间的复杂协同适应现象，减少网络中的过拟合现象。

## 9. 局限性

- 模型参数量较大，需要较高计算资源
- 对大规模标注数据存在较强依赖

## 10. 个人思考

在人工智能导论课程中曾学习过ReLU激活函数，但之前只是停留在公式层面的理解。通过阅读AlexNet论文，我进一步理解了ReLU在深度神经网络训练中的作用。此外，AlexNet的成功并不是依靠单一技术，而是数据规模、GPU计算能力、网络结构设计以及训练技巧共同推动的结果。这让我认识到，深度学习模型的发展不仅依赖算法创新，也依赖计算资源和数据规模的发展。







