# ResNet论文学习笔记

## 1. 论文基本信息

标题：Deep Residual Learning for Image Recognition

作者：Kaiming He\ Xiangyu Zhang\ Shaoqing Ren\ Jian Sun

## 2. 研究背景

随着深度学习的发展，卷积神经网络的深度不断增加，网络性能得到明显提升，然而研究者发现继续加深网络层数性能不一定越好，甚至会发生下降。

## 3. 研究问题

- 为什么增加网络深度会导致性能下降？

- 如何让网络深度增加真正带来性能提升？

## 4. 核心思想

论文提出残差学习，使得网络不直接学习目标函数而是学习输入宇目标之间的残差。

## 5. 方法详解

### 5.1 残差模块

ResNet的基本结构：

输入：x

经过几个卷积层：F(x)

然后与原始输入相加：y=F(x)+x

论文公式：y=F(x,{Wi})+x

其中：

- x：输入

- F(x)：卷积层学习的残差

- y：输出

### 5.2 捷径连接

ResNet通过shortcut连接实现：F(x)+x

shortcut具有以下特点：

- 不增加参数；
- 不增加计算量；
- 可以直接传播梯度。

### 5.3 Bottleneck结构

为了构建更深网络，论文提出Bottleneck结构。

普通残差块：

```
3×3 Conv
3×3 Conv
```

Bottleneck：

```
1×1 Conv
3×3 Conv
1×1 Conv
```

作用：

- 降低计算量；
- 减少参数；
- 支持构建50、101、152层网络。

## 6. 模型结构

输入图片-->7×7卷积-->最大池化-->多个Residual Block-->Global Average Pooling-->全连接层-->Softmax分类

## 7. 实验分析

### 7.1 ImageNet分类实验

数据：

- 1000类别；
- 128万训练图片；
- 5万验证图片。

实验结果：

| 模型           | Top-5错误率 |
| -------------- | ----------- |
| VGG            | 7.32%       |
| GoogLeNet      | 6.66%       |
| ResNet集成模型 | 3.57%       |

### 7.2 深度增加实验

普通CNN随着层数增加训练误差会升高。

ResNet随着深度增加性能继续提升。

### 7.3 CIFAR-10实验

论文进一步测试超过100层网络。

结果：

- ResNet-110仍然能够稳定训练。

- 甚至ResNet-1202也能够完成训练。

证明残差结构能够有效解决深层网络优化问题。

## 8. 创新点

### 8.1 提出残差学习框架

最大创新：从学习H(x)转变为学习F(x)+x，改变了深层CNN设计方式。

### 8.2 提出Shortcut Connection

利用Shortcut Connection：

- 改善梯度传播；
- 缓解梯度消失；
- 提高训练稳定性。

### 8.3 突破CNN深度限制

ResNet可以达到

- 152层ImageNet模型；
- CIFAR达到1000层级网络。

### 8.4 提出Bottleneck结构

降低深层网络计算成本，使超深网络成为可能。

## 9. 局限性

虽然ResNet影响巨大，但仍存在不足。

### 9.1 模型结构复杂

虽然解决了训练问题，但：

- 网络层数更多；
- 参数更多；
- 推理成本增加。

### 9.2 深度无限增加并不会无限提升性能

论文实验发现：

1202层ResNet在CIFAR-10上的测试性能反而下降。

原因：

可能存在：

- 过拟合；
- 模型容量过大。

### 9.3 残差连接理论解释仍不完善

论文提出：

残差学习更容易优化。

但是为什么一定有效，仍缺少严格理论证明。

### 9.4 依赖大量计算资源

ImageNet训练需要：

- 大规模数据；
- GPU计算资源；
- 长时间训练。

## 10. 个人思考

ResNet不仅是一种CNN结构，更是一种通用思想

这种思想后来影响：

- DenseNet；

- Transformer；

- Diffusion Model。

ResNet通过引入残差学习和Shortcut连接，解决了深层神经网络训练困难的问题，使网络能够从几十层扩展到数百甚至上千层，成为现代深度学习视觉模型的重要基础。